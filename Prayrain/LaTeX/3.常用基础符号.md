**常用基础符号（熟练掌握这4个，能写80%的公式）：**

1. **上下标**：上标用 `^`，下标用 `_`。
2. **分式（分数）**：使用 `\frac{分子}{分母}`。
3. **根号**：使用 `\sqrt{内容}`。
4. **乘除号**：不要用英文字母 `x`，用 `\times`（乘）和 `\div`（除）。


例子如下：
$2^3$与$2_3$ (上下标)
$\frac{3}{4}$ (分式)
$\sqrt{x^2}$ (根号)
$a \times b \div c$ (乘除号)






