
在记笔记时，经常会遇到分段函数（比如绝对值函数，或者条件概率）。
我们使用 `\begin{cases} ... \end{cases}`
其中：
- 用 `&` 来对齐条件。
- 用 `\\` 换行。
- 用 `\text{}` 插入中文条件。

例如:

```LaTeX
$$
|x| = 
\begin{cases}
x  &x>0 \\
-x  &x<0
\end{cases}
$$
```

效果如下：

$$
|x| = 
\begin{cases}
x  &x>0 \\
-x  &x<0
\end{cases}
$$